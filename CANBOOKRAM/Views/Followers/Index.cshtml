@model List<UserFriend>

<div class="card">
    <div class="card-header border-0 pb-0">
       <h5 class="card-title">Followers</h5> 
    </div>
    <div id="data-div" class="card-body">
        <partial name="_FollowersPartial" />
    </div>
</div>

<script>
function RefreshList() {
    $("#data-div").load("/Followers/RefreshList")
}

function RemoveFollower(id) {
    $.ajax({
         type: "POST",
         url: "/Followers/RemoveFollower?id=" + id,
         success: function (result) {
             RefreshList();                 
         },
         error: function (result) {
             alert('An error occured');
         }
     }); 
}
</script>